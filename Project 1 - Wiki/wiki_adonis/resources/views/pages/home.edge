<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> Encyclopedia </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body>
  <div class="flex h-full">
    <div class="px-3 py-10 bg-slate-50 flex-col w-72 space-y-5">
      <h2>Wiki</h2>
      <form>
        <input type="text" name="q"
          class="block w-full rounded-md border-0 py-1.5 pl-3 pr-5 text-gray-900 ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6"
          placeholder="Search Encyclopedia">
      </form>
      <nav class="flex flex-col space-y-1">
        <a href="{{ route('home') }}">Home</a>
        <a href="{{ route('entries.show', { slug: 'CSS'} )}}">Create New Page</a>
        <a href="{{ route('entries.show', { slug: 'Django'} )}}">Random Page</a>
      </nav>
    </div>
    <div class="w-full mt-6 px-6">
      <h1> All Pages </h1>
      <ul>
        @each(entry in entries)
        <li>
          <a href="{{ route('entries.show', { slug: entry.slug }) }}">
            {{ entry.slug }}
          </a>
        </li>
        @endeach
      </ul>
    </div>
  </div>
</body>

</html>